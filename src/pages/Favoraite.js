import FilmList from '../Components/FilmList';


const Favoraite = ({children}) => {
  return (
    <div className="container">
      {children}
    </div> 
  )
}

export default Favoraite;
